<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_custom" model="ir.ui.view">
        <field name="name">res.users.form.custom</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- Add the allowed_vendor_ids field to the user form -->
            <xpath expr="//field[@name='company_ids']" position="after">
                    <field name="allowed_vendor_ids" widget="many2many_tags"/>
                    <field name="passcode"/>
            </xpath>
        </field>
    </record>

    <record id="account_invoice_form_hide_price" model="ir.ui.view">
        <field name="name">account.move.form.inherit.hide.price</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[hasclass('oe_invoice_lines_tab')]" position='inside'>
                <field name='create_uid' invisible='1'/>
            </xpath>
            <xpath expr="//group[hasclass('oe_invoice_lines_tab')]" position='attributes'>
                <attribute name="invisible">partner_id in create_uid.allowed_vendor_ids</attribute>
            </xpath>
        </field>
    </record>

</odoo>
